# MCP 配置使用指南

本项目已配置 Model Context Protocol (MCP) 服务器，提供各种增强功能。

---

## 📦 已配置的 MCP 服务器

### 1. **Supabase MCP** ⭐ 重要

**功能**: 直接通过 MCP 操作 Supabase 数据库和 Storage

**配置**:
```json
{
  "command": "npx",
  "args": [
    "-y",
    "@supabase/mcp-server-supabase@latest",
    "--access-token",
    "sbp_21404d3f0604e14667c1374560863c7232a84497"
  ]
}
```

**可用操作**:
- ✅ 创建/读取/更新/删除数据库记录
- ✅ 执行 SQL 查询
- ✅ 创建 Storage Buckets
- ✅ 上传/下载/删除文件
- ✅ 设置 RLS 策略

**使用示例**:
```
你: 帮我在 Supabase 中创建 blog-images bucket
我: [使用 Supabase MCP 工具直接创建]

你: 列出 articles 表中的所有记录
我: [使用 Supabase MCP 查询数据]
```

### 2. **GitHub MCP**

**功能**: 直接操作 GitHub 仓库

**可用操作**:
- 创建/读取 issues
- 创建/合并 pull requests
- 搜索代码
- 管理 branches

### 3. **Vercel MCP**

**功能**: 管理 Vercel 部署

**可用操作**:
- 查看部署列表
- 获取部署详情
- 检查环境变量

### 4. **Memory MCP**

**功能**: 跨会话持久化记忆

**可用操作**:
- 存储重要信息
- 检索历史上下文
- 管理知识图谱

### 5. **Time MCP**

**功能**: 时间相关操作

**可用操作**:
- 获取当前时间
- 时区转换
- 时间计算

### 6. **Fetch MCP**

**功能**: 高级网页抓取（支持图片）

**可用操作**:
- 抓取网页内容
- 提取图片
- Markdown 转换

### 7. **Context7 MCP**

**功能**: 获取最新的库文档

**可用操作**:
- 查询最新 API 文档
- 获取代码示例
- 库版本信息

### 8. **Sequential Thinking MCP**

**功能**: 结构化思考和问题分解

### 9. **BrightData MCP**

**功能**: 数据抓取和代理服务

### 10. **Figma MCP**

**功能**: 获取 Figma 设计数据

### 11. **EdgeOne Pages MCP**

**功能**: 部署到 EdgeOne Pages（中国区）

### 12. **Chrome DevTools MCP**

**功能**: 浏览器自动化和调试

---

## 🚀 如何使用

### 方法 1: 在 Claude Code 中自动使用

MCP 工具会自动可用，只需自然语言请求：

```
你: 帮我在 Supabase 创建一个名为 test-bucket 的 Storage bucket
我: [自动使用 Supabase MCP 工具执行]
```

### 方法 2: 明确指定使用 MCP

```
你: 使用 Supabase MCP 查询 articles 表中浏览量最高的5篇文章
```

---

## 🔧 Supabase MCP 常用操作

### 创建 Storage Bucket

```
创建一个名为 "blog-images" 的公开 bucket
```

### 查询数据

```
查询 articles 表中所有分类为 "Tech" 的文章
```

### 执行 SQL

```
在 Supabase 中执行以下 SQL:
CREATE TABLE test (id serial primary key, name text);
```

### 上传文件

```
上传本地图片到 Supabase Storage 的 avatars bucket
```

### 设置 RLS 策略

```
为 articles 表创建允许公开读取的 RLS 策略
```

---

## ⚙️ 配置说明

### 项目配置位置

```
/Users/mac/Desktop/blog/.mcp.json
```

### 环境变量

Supabase 相关：
```bash
# .env 文件
VITE_SUPABASE_URL=https://gqvzempovmyfzqmyvojn.supabase.co
VITE_SUPABASE_KEY=sbp_21404d3f0604e14667c1374560863c7232a84497
```

### 更新配置

如需更新 MCP 配置：
1. 编辑 `.mcp.json`
2. 重启 Claude Code
3. 新配置自动生效

---

## 🔐 安全提示

**重要**: `.mcp.json` 包含敏感凭证，已在 `.gitignore` 中排除

确保以下文件不被提交：
- `.mcp.json`
- `.env`
- `.env.local`

---

## 🎯 实际应用场景

### 场景 1: 自动化配置 Supabase Storage

**之前**:
1. 手动打开 Supabase Dashboard
2. 点击 SQL Editor
3. 复制粘贴 SQL
4. 执行并检查错误
5. 手动验证 Buckets

**现在**:
```
你: 使用 storage.sql 的内容在 Supabase 中创建 buckets
我: [自动执行并验证]
```

### 场景 2: 快速查询和分析数据

```
你: 统计 moments 表中每天发布的动态数量，按日期降序排列
我: [直接查询并返回结果]
```

### 场景 3: 批量数据操作

```
你: 将所有分类为 "General" 的文章改为 "Tech"
我: [使用 Supabase MCP 批量更新]
```

---

## 📚 相关资源

- [Supabase MCP 文档](https://github.com/supabase/mcp-server-supabase)
- [MCP 协议规范](https://modelcontextprotocol.io/)
- [Claude Code MCP 指南](https://docs.anthropic.com/claude-code/mcp)

---

## 🆘 故障排查

### MCP 工具不可用

1. 检查 `.mcp.json` 语法是否正确
2. 验证 access token 是否有效
3. 重启 Claude Code

### Supabase 操作失败

1. 检查项目引用是否正确: `gqvzempovmyfzqmyvojn`
2. 验证 access token 权限
3. 查看 Supabase Dashboard 的 Logs

### 权限错误

某些操作可能需要更高权限的 token（service_role key）

---

## ✅ 下一步

使用 Supabase MCP 完成 Storage 配置：

```
帮我使用 Supabase MCP 创建以下 buckets:
1. blog-images (公开)
2. avatars (公开)

然后为这些 buckets 设置允许公开读取和上传的 RLS 策略
```

MCP 让 Supabase 配置自动化！🎉
